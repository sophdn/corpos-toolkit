surface = "knowledge"
action = "library_list_active"
purpose = "List every active library entry in the project's catalogue. Retired entries are excluded; use a different enumeration if you want the full set."
